{
	email {$ACME_EMAIL}
}

{$APP_DOMAIN} {
	encode zstd gzip

	@backend path /api/* /admin/* /api-auth/* /static/*
	reverse_proxy @backend backend:8000

	@websocket path /ws/*
	reverse_proxy @websocket backend:8000

	reverse_proxy frontend:3000
}
